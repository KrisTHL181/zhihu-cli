"""Legacy standalone scripts — replaced by the zhihu CLI.

These scripts still work when run directly (python -m zhihu_cli.legacy.<name>)
but the recommended interface is the `zhihu` CLI command.
"""
